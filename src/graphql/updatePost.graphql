mutation updatePost(
  $id: Int!
  $title: String!
  $content: String!
  $slug: String!
  $tags: String!
  $cover: String!
) {
  updatePost(
    id: $id
    title: $title
    content: $content
    tags: $tags
    slug: $slug
    cover: $cover
  ) {
    id
    slug
  }
}
